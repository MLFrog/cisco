const ctx = "/"
const springSecurityCtx = "v1"
const apiUrl = `${ctx}/v1`

export const API = {
    USER: {
        INFO: `${apiUrl}/user/info`
    },
    TEST: `${window.location.hostname}:8080/test`
}
