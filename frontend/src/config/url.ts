const ctx = "/"
const springSecurityCtx = "v1"
const apiUrl = `${ctx}/v1`

export const API = {
    USER: {
        INFO: `${apiUrl}/user/info`
    },
    RESERVATION: {
        INFO: `${apiUrl}/reservation/info`,
    },
    PARK: {
        LIST: `${apiUrl}/park/list`,
    },
    TEST: `/test`,

}
